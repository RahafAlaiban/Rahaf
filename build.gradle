plugins {
    id 'war'
  //  id 'org.springframework.boot' version '1.4.3.RELEASE'
}

group 'org.example'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    providedCompile fileTree(dir: 'lib', include: ['javaee-api-8.0.jar'])

    compileOnly "org.projectlombok:lombok:1.16.16"

}
